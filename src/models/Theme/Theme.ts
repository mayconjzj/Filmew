export type Theme = {
  border: {
    radius: string;
  },

  box: {
    shadow: string;
    padding: string;
  },

  colors: {
    white: string;
    black: string;
    gray: string;
    yellow: string;
  },

  transition: {
    default: string;
    fast: string;
  }
};
